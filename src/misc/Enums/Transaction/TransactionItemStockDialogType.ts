enum TransactionItemStockDialogType {
  /**
   * if the dialog is used to edit a transaction item stock
   */
  Edit = 0,
  /**
   * if the dialog is used to delete a transaction item stock
   */
  Delete = 1
}

export default TransactionItemStockDialogType
